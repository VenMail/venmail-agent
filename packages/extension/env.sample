# Venmail Extension Environment Sample
# Copy to .env within the extension build context if bundler supports dotenv.

# Optional Hunter API key used when the extension calls Hunter.io directly
HUNTER_API_KEY=

# Optional base URL for Venmail webapp proxy (e.g., http://localhost:3000)
VENMAIL_API_BASE_URL=
