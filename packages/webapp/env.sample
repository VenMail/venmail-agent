# Venmail Webapp Environment Sample
# Copy to .env.local for local development or configure environment variables in deployment.

# Required Hunter.io API key for server-side proxy at /api/hunter/verify
HUNTER_API_KEY=

# Optional log level (info, debug, warn, error)
LOG_LEVEL=info
